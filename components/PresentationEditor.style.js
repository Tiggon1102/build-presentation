const template = require('../config/styles.json');

const getMyStyle = () => ({
	presentationContainer: {
		display: 'flex',
		flexDirection: 'column',
		position: 'relative',
	},
});

export { getMyStyle };
