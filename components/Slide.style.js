const template = require('../config/styles.json');

const getMyStyle = () => ({
	slideContainer: {
		position: 'relative',
		marginTop: '20px',
		background: 'blue',
		height: '500px',
		width: '800px',
	},
});

export { getMyStyle };
