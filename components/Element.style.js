const template = require('../config/styles.json');

const getMyStyle = () => ({
	headlineElement: {},
	imageElement: {
		height: '100%',
		width: '100%',
	},
	textElement: {
		width: '100%',
		height: '100%',
	},
});

export { getMyStyle };
